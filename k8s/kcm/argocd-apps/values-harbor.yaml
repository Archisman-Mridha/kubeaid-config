harbor:
  persistence:
    persistentVolumeClaim:
      registry:
        size: 200Gi
        storageClass: rook-ceph-block
      jobservice:
        jobLog:
          size: 3Gi
          storageClass: rook-ceph-block

postgres:
  size: 6Gi
  storageClass: rook-ceph-block
